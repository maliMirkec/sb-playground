- var navData = [{ 'prefix': '🌐', 'title': 'Home', 'href': '/' }, { 'prefix': '🌐', 'title': 'Ribbon', 'href': '/ribbon/' }, { 'prefix': '📧', 'title': 'Contact', 'href': 'https://silvestar.typeform.com/to/jS4Qkz' }]

aside.sidebar
  nav
    ul
      each val, index in navData
        - var currentClass = val.title == pageTitle || val.title == parentTitle ? 'highlight' : ''
        li(class=currentClass)
          a(href=val.href)
            span= val.title == pageTitle || val.title == parentTitle ? '👉' : val.prefix
            span= val.title
